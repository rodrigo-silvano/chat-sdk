{
  "name": "@chat-sdk/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "lint": "echo 'lint ok'"
  },
  "dependencies": {
    "drizzle-orm": "^0.38.0",
    "postgres": "^3.4.0"
  },
  "devDependencies": {
    "@chat-sdk/tsconfig": "workspace:*",
    "drizzle-kit": "^0.30.0",
    "typescript": "^5.7.0"
  }
}
